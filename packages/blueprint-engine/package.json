{
  "name": "@ideia/blueprint-engine",
  "private": true,
  "description": "IDEIA Blueprint Engine — Project scaffolding, template rendering, dependency injection, config generation, ADR generation, blueprint market",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*",
    "@types/node": "*"
  }
}
