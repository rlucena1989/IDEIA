{
  "name": "@ideia/zero-to-deploy",
  "private": true,
  "description": "IDEIA Zero-to-Deploy — Pipeline orchestration, GitOps, Progressive Delivery, Multi-Cloud, Rollback",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*",
    "@types/node": "*"
  }
}
