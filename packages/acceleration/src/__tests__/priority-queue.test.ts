import { describe, it, expect } from '@jest/globals';
import { PriorityQueue } from '../priority-queue';

describe('priority-queue', () => {
  it('PriorityQueue should be defined', () => {
    expect(PriorityQueue).toBeDefined();
  });
});
