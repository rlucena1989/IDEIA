{
  "name": "@ideia/human-gate-pipeline",
  "private": true,
  "description": "IDEIA Human-in-the-Loop approval gate pipeline with multi-level escalation, notification routing, circuit breaker, and SHA-256 audit chain",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*"
  }
}
