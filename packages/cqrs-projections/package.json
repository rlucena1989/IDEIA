{
  "name": "@ideia/cqrs-projections",
  "private": true,
  "description": "CQRS Projections — Projection engine, live/batch projections, materialized views, competing consumers, cache invalidation, incremental materializer, warm standby, multi-region replication",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "@types/jest": "*"
  }
}
