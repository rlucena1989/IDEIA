{
  "name": "@ideia/pr-automation",
  "private": true,
  "description": "PR Automation Pipeline — 7-step PR automation: planning, code, tests, CI monitoring, auto-fix, review, merge gate",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*",
    "@types/node": "*"
  }
}
