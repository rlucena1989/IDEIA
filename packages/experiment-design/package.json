{
  "name": "@ideia/experiment-design",
  "private": true,
  "description": "IDEIA Experiment Design Framework — 4 randomization strategies (simple, blocked, stratified, adaptive) with sample size, power analysis, and balance diagnostics",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*"
  }
}
