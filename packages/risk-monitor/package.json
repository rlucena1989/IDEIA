{
  "name": "@ideia/risk-monitor",
  "private": true,
  "description": "IDEIA Continuous Risk Monitoring — real-time risk telemetry, dashboards, adaptive alerts, trend analysis, ML prediction, causal models, surface visualization",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*",
    "@ideia/bayesian-risk": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*"
  }
}
