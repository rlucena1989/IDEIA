{
  "name": "@ideia/package-managers",
  "private": true,
  "description": "Package Managers — gerenciamento de pacotes npm/pip/cargo/go, resolucao de dependencias, lockfile, auditoria",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "@types/jest": "*"
  }
}
