{
  "name": "@ideia/computer-use",
  "private": true,
  "description": "IDEIA Computer Use Engine — Playwright-based browser automation for autonomous agents with vision, recording, replay, and undo integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*",
    "playwright": "*"
  }
}
