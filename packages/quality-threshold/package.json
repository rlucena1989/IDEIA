{
  "name": "@ideia/quality-threshold",
  "private": true,
  "description": "ML Quality Threshold Adaptation — thresholds adaptativos para qualidade baseados em ML, deteccao de drift e ajuste automatico",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "@types/jest": "*"
  }
}
