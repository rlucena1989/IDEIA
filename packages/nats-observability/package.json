{
  "name": "@ideia/nats-observability",
  "private": true,
  "description": "NATS Observability & Monitoring — metricas de stream, consumer lag, DLQ health, latency percentis, Prometheus/Grafana",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "@types/jest": "*"
  }
}
