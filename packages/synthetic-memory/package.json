{
  "name": "@ideia/synthetic-memory",
  "private": true,
  "description": "Synthetic Memory Generation — geracao de dados sinteticos para testes de memoria, padrões de acesso e cenarios de retencao",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "@types/jest": "*"
  }
}
