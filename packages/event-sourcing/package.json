{
  "name": "@ideia/event-sourcing",
  "private": true,
  "description": "Event Sourcing — Aggregate repository, optimistic concurrency, snapshot, CQRS separation, Avro serialization, delta compression",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "@types/jest": "*"
  }
}
