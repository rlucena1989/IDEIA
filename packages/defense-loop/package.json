{
  "name": "@ideia/defense-loop",
  "private": true,
  "description": "IDEIA Defense Feedback Loop — closed-loop automated security with detect→analyze→respond→learn→adapt cycle and 7 frontier techniques (ATL, Constitutional, MAML, GNN, Federated, Formal, Counterfactual)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*",
    "@ideia/security-pipeline": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*"
  }
}
