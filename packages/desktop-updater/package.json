{
  "name": "@ideia/desktop-updater",
  "description": "IDEIA Desktop Auto-Update — Dual engine (Electron + Tauri), delta/differential updates (bsdiff), signature verification (Ed25519, SHA-512), staged rollout, rollback, resumable download, telemetry-driven canary releases",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "ts-jest": "^29.1.0",
    "jest": "^29.5.0",
    "@types/jest": "^29.5.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ]
}
