{
  "name": "@ideia/hybrid-search",
  "private": true,
  "description": "Hybrid Search — RRF fusion, cross-encoder re-ranking, ColBERT late interaction, SPLADE sparse retrieval, adaptive weight learning, learned sparse retrieval, distilled cross-encoder, adaptive fusion ranker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "@types/jest": "*"
  }
}
