{
  "name": "@ideia/nats-security",
  "private": true,
  "description": "NATS Authentication and Security — JWT/NKEY, mTLS, ACL, Auth Callout, Multi-tenant, Threat Modeling",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ideia/logger": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "*",
    "ts-jest": "*",
    "@types/jest": "*"
  }
}
