# Gerado por scripts/audit/regenerate-metrics.ts — NÃO editar à mão.
# Projeto: IDEIA (não confundir com o legado do diretório-pai do workspace)
project:
  name: ideia
  type: ide-platform
  default_language: typescript

architecture:
  pattern: clean-architecture + ddd + event-driven
  shell: eclipse-theia
  layers: 15

backend:
  framework: fastify
  adapter: fastify
  validation: zod
  orm: prisma
  database: postgresql
  auth: jwt

frontend:
  framework: react
  hosting: theia-widgets
  styling: tailwind
  ui: shadcn-ui
  forms: react-hook-form
  validation: zod
  server_state: tanstack-query

messaging:
  bus: nats-jetstream
  patterns: [pub-sub, req-rep, kv, dlq]

multiagent:
  orchestrator: langgraph
  llm: ollama-local

quality:
  lint: true
  typecheck: true
  unit_tests: true
  e2e_tests: true
  build_required: true
  coverage_min: 80

ai:
  context_strategy: minimal-relevant-files
  context_generator: scripts/audit/regenerate-metrics.ts
  require_artifact_manifest: true
  require_diff: true
  require_quality_gate: true
  allow_model_specific_rules: false

metrics:
  regenerated_by: scripts/audit/regenerate-metrics.ts
  src_packages: 292
  loc: 440083
  test_files: 1565
  cli_commands: 345
